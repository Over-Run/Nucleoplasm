plugins {
    id 'java'
}

archivesBaseName = getName()
println(getName())
group group_id
version project_version

allprojects {
    apply plugin: "java"
    apply plugin: 'maven-publish'

    group = group_id
}

