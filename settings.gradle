pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

rootProject.name = all

include ':module'
include("module:forge:1.19.4:nucleoplasm_api")

